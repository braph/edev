- application.cpp
  - Do a real terminal reset terminal on quit
  - Preload playlist
  - What to do when the database has updated?
- config.cpp
  - Replace in_list(T, std::initializer_list) by something like strstr?
- database.cpp
  - Style::styles() shall return an array of strings
- ui.cpp
  - Maybe change storage type of Pos and Size to short?
- downloads.cpp
  - Maybe add a --tries option to the Download base class?!
  - Small optimiation: Preallocate buffer for downloads (50*1024 for BrowsePage src)
- common.hpp
  - ProportionalGet rename to sth like `spannedGet`
  - Add tests for ^
- !!! Add browser
  if (selection)
    color = UI::Colors.get("list.item_selection");
  else if (index % 2 == 0)
    color = UI::Colors.get("list.item_even");
  else
    color = UI::Colors.get("list.item_odd");
- Config:
  - rename files from config.member.hpp to something shorter...
- view/progressbar.cpp
  - layout(): check if size has changed?
