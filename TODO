in_list() vs strstr!
create a playlist mockup
models
- Database: syles() return as array of stringzzz
- ui.hpp: change size type of Pos,Size to short?
- mouse section events
- downloads: add tries options?
- colorfader test in common.hpp
- ProportionalGet -> SpanView

- Browser:
  if (selection)
    color = UI::Colors.get("list.item_selection");
  else if (index % 2 == 0)
    color = UI::Colors.get("list.item_even");
  else
    color = UI::Colors.get("list.item_odd");

- Config: rename config.members.hpp etc.

- Reset terminal on quit

- view/progressbar.cpp: layout() check if size has changed

- Small optimizations:
  - buffer->reserve(50 * 1024) on downloading browsepage

- Use data().end() - data.begin() instead of size()/bits

  signal(SIGPIPE, SIG_IGN);
  signal(SIGABRT, SIG_IGN);

- Views::Progressbar: How to add a single wchar_t? 

- std::unique_ptr<> to store chars in palyinginfo

- Maybe
#if 0
TODO database.events.on(:update_finished, &browser.method(:reload))
TODO preload playlist
#endif
